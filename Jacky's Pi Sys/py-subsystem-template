The purpose of this template is to ease implementation of new subsystem types. 
Be sure to initialize and start this subsystem during main system initialization.
--- TEMPLATE ---

import importlib
subsys = importlib.import_module('path')

class MySubsystem(subsys.Subsystem):
    def __init__(self, thread_id, name):
        super().__init__(thread_id=thread_id, name=name)
        
        # Process any additional parameters, do initial setup.


    def loop():
        # Looping code block.


    # Define any other methods here.